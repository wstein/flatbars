{
  "name": "flatbars-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "FlatBars — a template-engine construction kit. PureScript monorepo: core library, CLI, and web playground.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "spago build",
    "test": "spago test -p flatbars && spago test -p kernel && spago test -p rawbars && spago test -p fullbars && spago test -p maxbars && spago test -p minbars && spago test -p fullbars-compile && spago test -p flatbars-json && spago test -p flatbars-js && spago test -p flatbars-compile && spago test -p linter && npm run check:catalog && npm run check:glossary && npm run check:isolation && npm run test:compile && npm run test:minbars-spec && npm run examples:verify && npm run check:tutorial-links && npm run check:conformance && npm run check:highlight && npm run check:bundle && npm run test:lab",
    "check:isolation": "node scripts/check-package-isolation.mjs",
    "check:glossary": "node scripts/check-glossary.mjs",
    "examples:verify": "spago build -p flatbars-cli && spago run -p flatbars-cli -- examples verify",
    "check:tutorial-links": "node scripts/check-tutorial-links.mjs",
    "gen:conformance": "node scripts/gen-conformance.mjs",
    "check:conformance": "node scripts/gen-conformance.mjs --check",
    "gen:highlight": "node scripts/gen-highlight.mjs",
    "check:highlight": "node scripts/gen-highlight.mjs --check",
    "check:bundle": "spago build -p flatbars-js && node scripts/check-bundle.mjs",
    "test:lab": "node lab/playground_utils.test.mjs && node lab/flatbars.test.mjs && node lab/open-in-lab.test.mjs && node lab/cm-flatbars.test.mjs && node lab/helpers.test.mjs",
    "cli": "spago run -p flatbars-cli",
    "lab": "node scripts/serve-lab.mjs",
    "docs": "antora docs/antora.yml",
    "gen:catalog": "spago build && node scripts/generate-helper-catalog.mjs",
    "check:catalog": "spago build && node scripts/generate-helper-catalog.mjs --check",
    "format": "purs-tidy format-in-place \"packages/*/src/**/*.purs\" \"packages/*/test/**/*.purs\"",
    "format:check": "purs-tidy check \"packages/*/src/**/*.purs\" \"packages/*/test/**/*.purs\"",
    "lint": "spago build --pedantic-packages",
    "clean": "rimraf output .spago",
    "test:compile": "spago build && node packages/compile/conformance.mjs",
    "test:minbars-spec": "spago build && node packages/minbars/test/spec-conformance.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "purescript": "^0.15.15",
    "purs-tidy": "^0.11.1",
    "rimraf": "^5.0.0",
    "spago": "^0.93.0"
  }
}
