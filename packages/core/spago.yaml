package:
  name: barebars
  publish:
    version: 0.1.0
    license: Apache-2.0
    location:
      githubOwner: barebars
      githubRepo: barebars
  dependencies:
    - prelude
    - arrays
    - lists
    - strings
    - maybe
    - either
    - foldable-traversable
    - ordered-collections
    - numbers
    - integers
    - record
    - parsing
    - control
    - transformers
  test:
    main: Test.BareBars.Main
    dependencies:
      - effect
      - console
      - assert
      - tuples
